# Frontend environment configuration
# Copy to ".env" (or ".env.local") and adjust as needed.

# Production: set this to your deployed PHP Slim backend URL
# e.g. https://unievent-backend.example.com
# Development: leave blank — Vite proxies /api/* to http://localhost:8080
VITE_API_BASE_URL=
